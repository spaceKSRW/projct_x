<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Homepage</title>
    <style>
      body {
        margin: 0;
        padding: 0;
        font-family: "Arial", sans-serif;
        color: #fff;
        background: url("https://img.freepik.com/free-vector/organization-abstract-concept_335657-3015.jpg")
          right/cover fixed no-repeat;
        background-size: contain;
      }

     

      ul {
        list-style: none;
        margin: 0;
        padding: 0;
      }

      li {
        display: inline-block;
        margin-right: 20px;
      }

      a {
        text-decoration: none;
        color: #fff;
        font-weight: bold;
        font-size: 18px;
      }

      /* Add spacing between anchor tags */
      li:not(:last-child) a {
        margin-right: 10px;
      }

      h1 {
        text-align: center;
        padding: 50px 0;
        font-size: 36px;
      }

      .user-details {
        display: flex;
        flex-direction: column;
        align-items: center;
        width: 650px;
        height: 800px;
        margin-left: 20px; /* Center the div horizontally */

        background-color: #fff; /* White background */
        padding: 20px;
        border-radius: 10px; /* Rounded corners */
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2); /* Subtle box shadow */
        font-family: "Open Sans", sans-serif; /* Clean and modern font */
        font-size: 16px;
        color: #333; /* Dark gray text color */
      }

      .user-details h2 {
        margin-bottom: 10px; /* Add some spacing below the heading */
      }

      .user-details p {
        margin-bottom: 5px; /* Add some spacing between paragraphs */
      }
      .user-details img {
        max-width: 100%;
        border-radius: 50%;
      }
      @media (max-width: 768px) {
  body {
    width: 50%;
  }
}

@media (min-width: 769px) and (max-width: 1024px) {
  body {
    width: 50%;
  }
}
      p{
        font-family: 'Franklin Gothic Medium', 'Arial Narrow', Arial, sans-serif;
        font-size: x-large;
        font-weight: 400;
      }
    </style>
  </head>
  <body>
    <%- include('navbar/header') %>
    <!-- User details and image section -->
    <div class="user-details">
      <!-- Replace the placeholder details with actual user details -->
      <div>
        <h2
          style="
            font-family: Impact, Haettenschweiler, 'Arial Narrow Bold',
              sans-serif;
            font-size: 400%;
          "
        >
          User Details
        </h2>
        <p >Full Name: <%= user.first_name + " " + user.last_name %></p>
        <p>Phone: <%= user.phone %></p>
        <p>Role: <%= user.role %></p>
        <p>Address: <%= user.address %></p>
        <p>Manager: <%= user.manager_id %></p>
        <p>Department: <%= user.department %></p>
        <p>Email: <%= user.email %></p>
        <p>Gender: <%= user.gender %></p>
        <p>Date of Birth: <%= user.dob %></p>
        <p>Date of Joining: <%= user.join_date %></p>
      </div>
    </div>

    <!-- Your homepage content goes here -->
  </body>
</html>
